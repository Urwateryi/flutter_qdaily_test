import 'package:flutter/material.dart';

class JoinUsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("JoinUsPage"),
      ),
    );
  }
}